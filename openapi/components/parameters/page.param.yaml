# -----------------------------------------------------------------------------
# openapi/components/parameters/page.param.yaml
# -----------------------------------------------------------------------------
name:     page
in:       query
schema:
  type:     integer
  minimum:  1
  default:  1
